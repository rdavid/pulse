# SPDX-FileCopyrightText: 2026 David Rabkin
# SPDX-License-Identifier: 0BSD
---
rules:
  # Actions are pinned to exact version tags rather than commit hashes.
  unpinned-uses:
    config:
      policies:
        '*': ref-pin
